<h1>Lists</h1>
<div class="container">
  <ul>
    <% @foods.each do |food| %>
       <li><%= link_to food.name, food_path(food)%></li>
    <% end %>
  </ul>

  <%= link_to "ADD a post", new_food_path %>
</div>
